rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acceso completo temporalmente para testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
